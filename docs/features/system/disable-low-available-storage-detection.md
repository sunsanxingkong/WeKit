# 禁用存储空间不足检测

> 「隐藏应用列表」等隐藏 Root 模块有时会使应用获取到的可用空间不正确, 而微信在可用空间不足时会强制要求清理空间才可继续使用, 本功能移除了该限制

## 类别

系统与隐私

## 类型

可开关

## 描述

「隐藏应用列表」等隐藏 Root 的模块有时会使应用获取到的可用空间不正确, 导致微信误认为设备存储空间不足, 从而强制要求用户清理空间才可继续使用。启用本功能后移除该限制, 避免因可用空间误报导致微信无法正常使用。

## 使用方法

在模块设置中启用
