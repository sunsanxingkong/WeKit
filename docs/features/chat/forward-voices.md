# 转发语音

> 在语音消息长按菜单添加转发按钮, 可向好友或群聊批量转发语音

## 类别

聊天

## 类型

可开关

## 描述

在聊天中语音消息的长按菜单中添加「转发」按钮。点击后弹出联系人选择器, 可选择多个好友或群聊, 将语音消息批量转发给选中的对象。

转发时会读取语音文件的原始路径, 使用微信 API 发送语音消息, 接收方收到后可直接播放。支持同时转发给多个联系人。

## 使用方法

在模块设置中启用。在聊天中长按任意语音消息, 菜单中选择「转发」, 选择目标联系人后确认即可
