# 一键撤回并重新编辑

> 向消息长按菜单添加菜单项, 可快捷撤回消息并将文本内容加入输入框

## 类别

聊天

## 类型

可开关

## 描述

在文本消息的长按菜单中添加「编辑」按钮。点击后自动执行撤回操作, 同时将撤回消息的文本内容自动填入输入框, 并自动弹出键盘, 方便直接修改后重新发送。

省去了手动长按 → 撤回 → 再手动复制粘贴的步骤。

## 使用方法

在模块设置中启用。在聊天中长按自己发送的文本消息, 选择「编辑」即可
